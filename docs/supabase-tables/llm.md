# LLM 관련 테이블

원격 Supabase에 이미 생성된 스키마다. (PascalCase 식별자, quoted `"TableName"`)

## 스키마 정규화 (2026-07-22, `20260722160000_llm_provider_model_normalize.sql`)

이전에는 `LLMProvider` 한 테이블에 **모델마다 행**이 있고 모든 행이 같은 `ApiKey`를 복제하고 있었다.
정규화 후:

```
LLMProvider (1) ──< LLMModel (N)           제공자 1개(예: Google)가 여러 모델 보유, ApiKey는 제공자에만
                ├──< LLMUserPermission (N)  사용자×제공자 승인·할당 (모델 단위 아님)
                ├──< LLMUserQuota (N)       사용자×제공자×월 누적
                ├──< LLMUserMonthlyAllocation (N)  월별 할당 이력(조회용)
                ├──< LLMTokenHistory (N)    ProviderID + ModelID
                └──< ChatSession (N)        ProviderID + ModelID → ChatMessage
```

| 테이블 | 역할 |
|--------|------|
| **LLMProvider** | 제공자(ApiKey 소유자). 컬럼: `ProviderID`, `ProviderName`, `ApiKey`, `RegDate` 만. 시드: `ProviderID=1` `Google`, `ProviderID=2` `Groq` |
| **LLMModel** | 개별 모델(구 LLMProvider rename). PK=`ModelID`. `ProviderID` FK. **ApiKey/DailyLimit/MonthlyLimit/ProviderName 없음** |
| **LLMSystemPrompt** | AI Lab **전역** 시스템 프롬프트. 활성 항목이 **모든 모델** 채팅에 동일 적용 |
| **LLMUserPermission** | 사용자×**제공자** 승인·할당 |
| **LLMUserQuota** | 사용자×**제공자**×월 누적. 정규화 시 모델별 행을 SUM으로 합침 → 제공자 1개면 `(SerialNo,TargetMonth)` 사실상 유일 |
| **LLMTokenHistory** | 호출 이력. `ProviderID`(제공자) + `ModelID`(실제 모델) |
| **LLMCallAttemptLog** | Gemini **시도별** 진단 로그(`llm-chat-send`). Edge Logs 없이도 실패 원인 조회 |
| **LLMAiLabSuggestionCategory** | AI Lab 빈 화면 추천 질문 **카테고리** (`AnswerMode`) |
| **LLMAiLabSuggestionPrompt** | 카테고리별 추천 질문 문구 |
| **ChatSession** | `ProviderID` + `ModelID` — 세션에 마지막 사용 모델 기록. **실제 호출은 클라이언트가 보낸 `modelId` 우선**(모델 변경 시 세션 값도 갱신) |

채팅 요청 body는 `modelId`(클라이언트가 고른 모델). Edge Function/`nrm_rpc_chat_prepare_turn`이 `LLMModel`→`LLMProvider` join으로 ApiKey를 얻고, 권한/쿼터는 **provider** 단위로 체크한다.

`SerialNo`는 사용자 일련번호다. `LLMUserQuota`·`LLMTokenHistory`·`LLMUserPermission`의 `SerialNo`는 **varchar**로, 앱 `getNrmAppSerialNo()` 원문을 그대로 저장한다.

---

## LLMProvider

LLM 제공자(ApiKey 소유자). 정규화 후 **모델 목록이 아니다**.

| 컬럼 | 타입 | 기본값 | NULL | 설명 |
|------|------|--------|------|------|
| `ProviderID` | `bigint` IDENTITY PK | — | NO | LLM 제공자 고유번호 |
| `ProviderName` | `varchar` | — | NO | 제공자명 (예: `Google`, `Groq`) |
| `ApiKey` | `text` | — | NO | LLM API Key (이 제공자의 전체 모델이 공유) |
| `RegDate` | `timestamptz` | `now()` | NO | 등록일시 |

RLS: SELECT만 anon/authenticated. `ApiKey`는 컬럼 단위 REVOKE — 앱 `select`로는 못 읽고 Edge Function(service_role)만 읽는다.

### DDL

```sql
CREATE TABLE public."LLMProvider"
(
    "ProviderID"    bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    "ProviderName"  varchar NOT NULL,
    "ApiKey"        text NOT NULL,
    "RegDate"       timestamptz NOT NULL DEFAULT now()
);
```

---

## LLMModel

개별 LLM 모델(구 `LLMProvider` 테이블 rename + PK `ProviderID`→`ModelID`).

| 컬럼 | 타입 | 기본값 | NULL | 설명 |
|------|------|--------|------|------|
| `ModelID` | `bigint` IDENTITY PK | — | NO | LLM 모델 고유번호 |
| `ProviderID` | `bigint` | — | NO | 소속 제공자 (`LLMProvider.ProviderID`) |
| `Type` | `varchar` | — | NO | `LLM`/`Embedding`/`Image`/`Video`/`TTS` |
| `ModelName` | `varchar` | — | NO | API 모델명 (예: `models/gemini-3.5-flash`) |
| `ModelDisplayName` | `text` | — | NO | UI 표시 이름 |
| `Version` | `varchar` | — | NO | 모델 버전 |
| `Description` | `text` | — | YES | 설명 |
| `IsActive` | `boolean` | `true` | NO | 사용 여부 |
| `RegDate` | `timestamptz` | `now()` | NO | 등록일시 |

**제거된 컬럼(정규화):** `ApiKey`, `DailyLimit`, `MonthlyLimit`, `ProviderName` — 제공자 정보는 `LLMProvider` join.

**현재 활성 채팅 모델 (2026-07-23):** Groq 우선 — `openai/gpt-oss-120b`(`ModelID=1002`), `qwen/qwen3.6-27b`(`1001`), `llama-3.3-70b-versatile`(`1000`) + Google Gemini `models/gemini-3.1-flash-lite`(`21`), `models/gemini-3.5-flash`(`28`), `models/gemini-3.5-flash-lite`(`55`), `models/gemini-3.6-flash`(`56`).

AI Lab 모델 선택 정렬(`sortLlmModelsForPicker`): `IsActive=true` 상단 → `ModelID` 내림차순 → `ProviderID` 내림차순. (Groq 활성 3종이 1002→1000이라 피커 최상단)

관리자 시드 RPC: `nrm_rpc_admin_replace_llm_models` (구 `nrm_rpc_admin_replace_llm_providers` DROP).

---

## LLMSystemPrompt

AI Lab 전역 시스템 프롬프트. 관리자페이지 「AI 시스템 프롬프트 설정」에서 CRUD.
`IsActive=true`인 행을 `SortOrder`→`PromptID` 오름차순으로 이어 붙여, Edge Function(`llm-chat-send`)이 **모든 Provider·모든 모델**의 시스템 지시에 동일하게 넣는다(모델/제공자별 분기 없음).
- Google(Gemini): `systemInstruction`
- Groq: OpenAI 호환 `messages`의 `role=system`
조합은 Agent `PromptBuilder`(`agent/prompt/builder.ts`)가 Section(id/priority)으로 조립한다. Planner(`agent/planner.ts`)가 Intent·Context·Tools에 맞춰 체인을 탄다.

조합 순서(2026-07-24 Agent):
1. 코드 `[CURRENT_DATETIME]`(Asia/Seoul)
2. `[ROLE]` / DB 활성 `LLMSystemPrompt` → `[ADMIN_SYSTEM_PROMPT]`
3. Intent별 규칙 섹션(`WEB_SEARCH` / `DOWNLOAD` / `RECOMMENDATION` / `RAG` / `APP_FAQ` / `TOOLS` / `OUTPUT`)
4. `[RETRIEVED_CONTEXT]` — FAQ/Vector 수집기 결과(현재 스텁, 확장 포인트)

시드(2026-07-23): `현재 시각 해석 규칙`(SortOrder=1).  
`Google Search 사용 규칙`(SortOrder=2)은 **비활성** — 검색 여부는 **Intent Classifier**(`needSearch`)가 결정한다(UI 토글 제거, 2026-07-24).
최초 시드는 `20260723110000_llm_system_prompt_seed.sql`(당시 10/20), `20260723120000_llm_system_prompt_sortorder_1_2.sql`에서 1/2로 정리.
`SortOrder`는 10단위 강제 아님 — 정수 오름차순이면 된다.

추가(2026-07-23, `20260723140000_llm_system_prompt_music_assistant.sql`):
- SortOrder=2 `Google Search 사용 규칙` 본문 보강(차트·신곡·일정 등) — 이후 UI 토글로 대체·비활성
- SortOrder=3 `역할 및 답변 범위` (음악 전용 + 비음악 거절 한 줄)
- SortOrder=4 `앱 기능 안내 및 도구 호출` (다운로드·삭제·가사·메타데이터는 Tool 필수; 거짓말 금지)
- SortOrder=5 `추천 및 응답 스타일`
앱 데이터/벡터 우선 규칙은 아직 미구현이라 넣지 않음. 날짜 규칙은 SortOrder=1 + 코드 `[CURRENT_DATETIME]`에 위임.

추가(2026-07-23, `20260723170000_llm_system_prompt_creator.sql`):
- SortOrder=6 `앱 제작자 및 관리자` — 앱 소개 질문 시 만든 사람·관리자=윤현상, 인물 소개 포함

완화(2026-07-23, `20260723180000_llm_system_prompt_relax_scope.sql`):
- SortOrder=3 `역할 및 답변 범위` — 음악 + **앱 관련 전부** 답변. 완전 무관 주제만 한 줄 거절. 애매하면 거절하지 않음
- SortOrder=4 `앱 기능 안내 및 도구 호출` — 음악 다운로드·삭제·가사생성/삭제/번역·오디오 메타데이터 추가·편집은 제공된 Tool 사용. 직접 했다고 거짓말 금지

보강(2026-07-23, `20260723200000_llm_system_prompt_no_leak.sql`):
- SortOrder=5·6 — 시스템 지시문·`<think>` 등 내부 추론을 사용자에게 노출하지 않도록 명시. 제작자(윤현상) 질문은 자연어로만 답

보강(2026-07-23, `20260723210000_llm_system_prompt_tools_guard.sql`):
- SortOrder=2 — `google_search`는 **요청에 도구가 있을 때만** 호출. 없으면 tool call 금지(Groq 400 `tool_use_failed` 방지)

갱신(2026-07-24, `20260724150000_llm_system_prompt_tools_required.sql`):
- SortOrder=4 / PromptID=4 `앱 기능 안내 및 도구 호출` — 음악 다운로드·삭제·가사생성·가사삭제·가사번역·오디오 메타데이터는 **이번 요청에 Tool이 있을 때만** Tool 사용. Tool 없으면 직접 했다고 말하지 말고 앱 기능 안내. 거짓말 금지.

| 컬럼 | 타입 | 기본값 | NULL | 설명 |
|------|------|--------|------|------|
| `PromptID` | `bigint` IDENTITY PK | — | NO | 시스템 프롬프트 고유번호 |
| `Title` | `varchar` | — | NO | 관리자 UI 표시용 제목 |
| `Content` | `text` | — | NO | 모델에 전달할 본문 |
| `SortOrder` | `integer` | `0` | NO | 적용 순서(오름차순, 1·2·3 등 연속값 OK) |
| `IsActive` | `boolean` | `true` | NO | 활성 여부 |
| `UpdatedBySerialNo` | `varchar` | — | YES | 마지막 저장 관리자 SerialNo |
| `RegDate` | `timestamptz` | `now()` | NO | 등록일시 |
| `UpdateDate` | `timestamptz` | `now()` | NO | 최종 수정일시 |

RLS: SELECT만 anon/authenticated. 쓰기는 admin RPC:
- `nrm_rpc_admin_upsert_llm_system_prompt` (`p_prompt_id` NULL=INSERT)
- `nrm_rpc_admin_delete_llm_system_prompt`

마이그레이션: `20260723100000_llm_system_prompt.sql`, 시드 `20260723110000_llm_system_prompt_seed.sql`.

---

## LLMAiLabSuggestionCategory / LLMAiLabSuggestionPrompt

AI Lab **빈 대화 화면** 추천 질문. 앱이 활성 카테고리·프롬프트를 읽어, 적격 카테고리에서 **최대 3개**(카테고리당 1질문)를 무작위로 보여 준다. 탭하면 해당 `PromptText`를 그대로 전송한다.

| AnswerMode | 의미 | UI 노출 조건 |
| --- | --- | --- |
| `plain` | 일반(검색 불필요 질문 후보) | 항상 |
| `web_search` | 최신/차트 등 검색이 필요할 수 있는 질문 후보 | 항상(실제 검색은 서버 Intent가 결정) |
| `vector_plain` | 벡터DB 기반 | 앱 플래그 `NRM_AI_LAB_VECTOR_SUGGESTIONS_ENABLED` (현재 false) |
| `vector_web` | 벡터DB + 검색 가능 질문 | 벡터 플래그 ON일 때만 |

시드 카테고리: `music_recommend`(음악 추천/plain), `upcoming_release`(발매 예정곡/web_search), `ai_personal_web`(AI 활용/vector_web), `user_personal`(사용자 맞춤/vector_plain), `app_intro`(앱 소개/plain).

마이그레이션: `20260723240000_llm_ailab_suggestion_prompts.sql`. RLS: SELECT만 anon/authenticated.

### LLMAiLabSuggestionCategory

| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| `CategoryID` | `bigint` PK | |
| `CategoryCode` | `varchar` UNIQUE | 코드 |
| `Title` | `varchar` | 표시명 |
| `AnswerMode` | `varchar` | 위 표 |
| `SortOrder` / `IsActive` | | |

### LLMAiLabSuggestionPrompt

| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| `PromptID` | `bigint` PK | |
| `CategoryID` | `bigint` FK | |
| `PromptText` | `text` | 칩·전송 문구 |
| `SortOrder` / `IsActive` | | |

---

## LLMCallAttemptLog

`llm-chat-send`가 Gemini `generateContent`를 **시도할 때마다** 1행 INSERT (service_role).
앱 로그 `ailab.llmSend`의 `requestId` = `RequestID`로 조인하면, Dashboard Edge Logs 없이도 실패 원인을 SQL로 볼 수 있다.

| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| `LogID` | `bigint` PK | |
| `RequestID` | `uuid` | Edge 요청 UUID |
| `SerialNo` | `varchar` | 요청 사용자 |
| `SessionID` | `bigint` | 채팅 세션 |
| `ModelID` / `ModelName` | | 호출 모델 |
| `AttemptIndex` / `AttemptLabel` | | 1부터, `search`/`plain`/`plain_retry` |
| `WithSearch` | `boolean` | `google_search` tool 포함 여부 |
| `HttpStatus` | `integer` | Gemini HTTP status (네트워크 예외면 NULL) |
| `Ok` | `boolean` | 해당 시도 성공 여부 |
| `ErrorKind` / `ErrorMessage` | | `auth`/`other`/`network`/`timeout` + 메시지(최대 ~800자) |
| `FinishReason` / `BlockReason` | | 빈 응답 진단용 |
| `GroundedQueryCount` | `integer` | |
| `ElapsedMs` / `MaxOutputTokens` | | |
| `UserMessagePreview` | `varchar` | 60자 미리보기 |
| `NeedsWebSearch` | `boolean` | 서버 휴리스틱 |
| `RegDate` | `timestamptz` | |

RLS: anon/authenticated **SELECT만**. INSERT는 Edge(service_role).

마이그레이션: `20260723130000_llm_call_attempt_log.sql`.

---

## LLMUserQuota

사용자×**제공자**×월 LLM 토큰 사용량 (누적). 정규화 후 ProviderID는 실제 제공자(예: Google=1)다. 과거 모델별로 쪼개져 있던 행은 SUM으로 합쳐졌다.

| 컬럼 | 타입 | 기본값 | NULL | 설명 |
|------|------|--------|------|------|
| `QuotaID` | `bigint` IDENTITY | — | NO | 사용량 고유번호 **(복합 PK)** |
| `SerialNo` | `varchar` | — | NO | 사용자 일련번호 (앱 `getNrmAppSerialNo()` 원문, 예: `"admin"`, `"1092452918"`) **(복합 PK)** |
| `ProviderID` | `bigint` | — | NO | LLM 제공자 고유번호 (`LLMProvider.ProviderID`) |
| `TargetMonth` | `varchar(6)` | — | NO | 사용월 (`YYYYMM`) |
| `InputToken` | `bigint` | `0` | NO | 누적 입력 토큰 (LLM에 보낸 토큰 수) |
| `OutputToken` | `bigint` | `0` | NO | 누적 출력 토큰 (LLM이 생성한 토큰 수) |
| `TotalToken` | `bigint` | `0` | NO | 누적 전체 토큰 (`InputToken + OutputToken`) |
| `RegDate` | `timestamptz` | `now()` | NO | 생성일시 |
| `UpdateDate` | `timestamptz` | `now()` | NO | 최종 수정일시 |

### PK·제약·인덱스

- **PK** `PK_LLMUserQuota` (`QuotaID`, `SerialNo`)
- **UNIQUE** `UX_LLMUserQuota` (`SerialNo`, `ProviderID`, `TargetMonth`)  
  → 동일 사용자·동일 제공자·동일 월은 **1건만** 존재.

### DDL

```sql
CREATE TABLE public."LLMUserQuota"
(
    "QuotaID"       bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    "SerialNo"      varchar NOT NULL,
    "ProviderID"    bigint NOT NULL,
    "TargetMonth"   varchar(6) NOT NULL,
    "InputToken"    bigint NOT NULL DEFAULT 0,
    "OutputToken"   bigint NOT NULL DEFAULT 0,
    "TotalToken"    bigint NOT NULL DEFAULT 0,
    "RegDate"       timestamptz NOT NULL DEFAULT now(),
    "UpdateDate"    timestamptz NOT NULL DEFAULT now(),
    CONSTRAINT "PK_LLMUserQuota" PRIMARY KEY ("QuotaID", "SerialNo")
);

COMMENT ON TABLE public."LLMUserQuota"
IS '사용자 월별 LLM 토큰 사용량';

COMMENT ON COLUMN public."LLMUserQuota"."QuotaID"
IS '사용량 고유번호';

COMMENT ON COLUMN public."LLMUserQuota"."SerialNo"
IS '사용자 일련번호 (앱 SerialNo 원문, 예: admin, 1092452918)';

COMMENT ON COLUMN public."LLMUserQuota"."ProviderID"
IS 'LLM 제공자 고유번호';

COMMENT ON COLUMN public."LLMUserQuota"."TargetMonth"
IS '사용월 (YYYYMM)';

COMMENT ON COLUMN public."LLMUserQuota"."InputToken"
IS '누적 입력 토큰(LLM에게 보낸 토큰 수)';

COMMENT ON COLUMN public."LLMUserQuota"."OutputToken"
IS '누적 출력 토큰(LLM이 생성한 토큰 수)';

COMMENT ON COLUMN public."LLMUserQuota"."TotalToken"
IS '누적 전체 토큰(InputToken + OutputToken)';

COMMENT ON COLUMN public."LLMUserQuota"."RegDate"
IS '생성일시';

COMMENT ON COLUMN public."LLMUserQuota"."UpdateDate"
IS '최종 수정일시';

CREATE UNIQUE INDEX "UX_LLMUserQuota"
ON public."LLMUserQuota"
(
    "SerialNo",
    "ProviderID",
    "TargetMonth"
);
```

---

## LLMTokenHistory

LLM 호출 이력 (호출 단위).

| 컬럼 | 타입 | 기본값 | NULL | 설명 |
|------|------|--------|------|------|
| `HistoryID` | `bigint` IDENTITY | — | NO | 호출 이력 고유번호 **(복합 PK)** |
| `SerialNo` | `varchar` | — | NO | 사용자 일련번호 (앱 `getNrmAppSerialNo()` 원문, 예: `"admin"`, `"1092452918"`) **(복합 PK)** |
| `ProviderID` | `bigint` | — | NO | LLM 제공자 고유번호 |
| `ModelID` | `bigint` | — | NO | 실제 호출에 사용한 LLM 모델 (`LLMModel.ModelID`) |
| `InputToken` | `integer` | `0` | NO | 입력 토큰 (해당 호출) |
| `OutputToken` | `integer` | `0` | NO | 출력 토큰 (해당 호출) |
| `TotalToken` | `integer` | `0` | NO | 전체 토큰 (`InputToken + OutputToken`) |
| `isSuccess` | `boolean` | `false` | NO | 요청에 대한 응답이 성공인지 여부 |
| `RegDate` | `timestamptz` | `now()` | NO | 호출 일시 |

### PK·인덱스

- **PK** `PK_LLMTokenHistory` (`HistoryID`, `SerialNo`)
- `IX_LLMTokenHistory_SerialNo` (`SerialNo`) — `SerialNo` 단독 조회용 (복합 PK 2번째 컬럼이라 별도 인덱스 유지)
- `IX_LLMTokenHistory_RegDate` (`RegDate`)

### DDL

```sql
CREATE TABLE public."LLMTokenHistory"
(
    "HistoryID"     bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    "SerialNo"      varchar NOT NULL,
    "ProviderID"    bigint NOT NULL,
    "InputToken"    integer NOT NULL DEFAULT 0,
    "OutputToken"   integer NOT NULL DEFAULT 0,
    "TotalToken"    integer NOT NULL DEFAULT 0,
    "isSuccess"     boolean NOT NULL DEFAULT false,
    "RegDate"       timestamptz NOT NULL DEFAULT now(),
    CONSTRAINT "PK_LLMTokenHistory" PRIMARY KEY ("HistoryID", "SerialNo")
);

COMMENT ON TABLE public."LLMTokenHistory"
IS 'LLM 호출 이력';

COMMENT ON COLUMN public."LLMTokenHistory"."HistoryID"
IS 'LLM 호출 이력 고유번호';

COMMENT ON COLUMN public."LLMTokenHistory"."SerialNo"
IS '사용자 일련번호 (앱 SerialNo 원문, 예: admin, 1092452918)';

COMMENT ON COLUMN public."LLMTokenHistory"."ProviderID"
IS 'LLM 제공자 고유번호';

COMMENT ON COLUMN public."LLMTokenHistory"."InputToken"
IS '입력 토큰(LLM에게 보낸 토큰 수)';

COMMENT ON COLUMN public."LLMTokenHistory"."OutputToken"
IS '출력 토큰(LLM이 생성한 토큰 수)';

COMMENT ON COLUMN public."LLMTokenHistory"."TotalToken"
IS '전체 토큰(InputToken + OutputToken)';

COMMENT ON COLUMN public."LLMTokenHistory"."isSuccess"
IS '요청에 대한 응답이 성공인지 여부';

COMMENT ON COLUMN public."LLMTokenHistory"."RegDate"
IS '호출 일시';

CREATE INDEX "IX_LLMTokenHistory_SerialNo"
ON public."LLMTokenHistory" ("SerialNo");

CREATE INDEX "IX_LLMTokenHistory_RegDate"
ON public."LLMTokenHistory" ("RegDate");
```

---

## LLMUserPermission

사용자별 LLM 사용 권한 및 토큰 할당.

| 컬럼 | 타입 | 기본값 | NULL | 설명 |
|------|------|--------|------|------|
| `PermissionID` | `bigint` IDENTITY | — | NO | 권한 고유번호 **(복합 PK)** |
| `SerialNo` | `varchar` | — | NO | 사용자 일련번호 (앱 `getNrmAppSerialNo()` 원문, 예: `"admin"`, `"1092452918"`) **(복합 PK)** |
| `ProviderID` | `bigint` | — | NO | LLM 제공자 고유번호 (`LLMProvider.ProviderID`) |
| `IsApproved` | `boolean` | `false` | NO | LLM 사용 승인 여부 |
| `AllocatedToken` | `bigint` | `0` | NO | 관리자가 할당한 총 사용 가능 토큰 |
| `ApprovedDate` | `timestamptz` | — | YES | 승인 일시 |
| `RegDate` | `timestamptz` | `now()` | NO | 등록 일시 |
| `UpdateDate` | `timestamptz` | `now()` | NO | 최종 수정 일시 |

### PK·제약·인덱스

- **PK** `PK_LLMUserPermission` (`PermissionID`, `SerialNo`)
- **UNIQUE** `UX_LLMUserPermission` (`SerialNo`, `ProviderID`)  
  → 동일 사용자·동일 제공자는 **1건만** 존재.

### DDL

```sql
CREATE TABLE public."LLMUserPermission"
(
    "PermissionID"      bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    "SerialNo"          varchar NOT NULL,
    "ProviderID"        bigint NOT NULL,

    "IsApproved"        boolean NOT NULL DEFAULT false,

    "AllocatedToken"    bigint NOT NULL DEFAULT 0,

    "ApprovedDate"      timestamptz,

    "RegDate"           timestamptz NOT NULL DEFAULT now(),
    "UpdateDate"        timestamptz NOT NULL DEFAULT now(),
    CONSTRAINT "PK_LLMUserPermission" PRIMARY KEY ("PermissionID", "SerialNo")
);

COMMENT ON TABLE public."LLMUserPermission"
IS '사용자별 LLM 사용 권한 및 토큰 할당';

COMMENT ON COLUMN public."LLMUserPermission"."PermissionID"
IS '권한 고유번호';

COMMENT ON COLUMN public."LLMUserPermission"."SerialNo"
IS '사용자 일련번호 (앱 SerialNo 원문, 예: admin, 1092452918)';

COMMENT ON COLUMN public."LLMUserPermission"."ProviderID"
IS 'LLM 제공자 고유번호';

COMMENT ON COLUMN public."LLMUserPermission"."IsApproved"
IS 'LLM 사용 승인 여부';

COMMENT ON COLUMN public."LLMUserPermission"."AllocatedToken"
IS '관리자가 할당한 총 사용 가능 토큰';

COMMENT ON COLUMN public."LLMUserPermission"."ApprovedDate"
IS '승인 일시';

COMMENT ON COLUMN public."LLMUserPermission"."RegDate"
IS '등록 일시';

COMMENT ON COLUMN public."LLMUserPermission"."UpdateDate"
IS '최종 수정 일시';

CREATE UNIQUE INDEX "UX_LLMUserPermission"
ON public."LLMUserPermission"
(
    "SerialNo",
    "ProviderID"
);
```

---

## 보안 — RLS·컬럼 권한 (`20260721120000_llm_chat_security_harden.sql`)

이 6개 테이블은 생성 시 RLS를 켜지 않아 "쓰기는 RPC로만"이라는 원칙이 적용되지 않고 있었다. 아래로 강화:

- `LLMProvider`/`LLMUserPermission`/`LLMUserQuota`/`LLMTokenHistory`/`ChatSession`/`ChatMessage` 모두 `ENABLE ROW LEVEL SECURITY` + `SELECT` 정책만 anon/authenticated에 허용(`USING (true)`). **쓰기 정책은 없음** → 모든 쓰기는 `nrm_rpc_*` (SECURITY DEFINER) 경유.
- `LLMProvider."ApiKey"`는 RLS(row 단위)로 못 숨기므로 **컬럼 단위**로 `REVOKE SELECT ... FROM anon, authenticated` 후 나머지 컬럼만 `GRANT SELECT (...)`. 앱이 실수로 `select=*`를 하더라도 DB가 `ApiKey`를 막는다.

## AI Lab Agent — Registry / DAG (2026-07-24)

SSOT: `supabase/functions/llm-chat-send/agent/`

```
Intent → Planner → DAG(ExecutionGraph)
  → Executor (topological waves, parallel nodes)
  → ProviderFactory.get(id).stream → NormalizedResponse
```

| 확장 | 등록 |
| --- | --- |
| Provider | `registerProviderFactory` / `ProviderFactory.get` |
| Tool | `registerToolFactory` / Tool version+schema |
| Context | cost / estimatedLatencyMs / supportsParallelExecution |
| Graph | `graph/build.ts` DAG |
| Retry | `policy/retry.ts` |
| Merge | `context/merge.ts` |

Executor에 Provider 분기 없음. AbortController 취소, Token 버킷 동적 재분배, TokenUsageBreakdown.

## AI Lab Agent — Production Ops 계약 (2026-07-24)

구조는 완성 단계. 이후는 **운영 품질**과 **실제 기능 부착** 우선(추가 추상화 지양).

```
AgentRequest
  → FeatureFlags / Experiment / PromptVersion
  → InputGuard
  → (QuestionCache 훅)
  → Planner → ExecutionGraph → Executor
  → OutputGuard
  → AgentResponse (+ Evaluation / Metrics)
  → diag.agentResponse · 구조화 로그
```

| 모듈 | 경로 | 상태 |
| --- | --- | --- |
| 계약 | `agent/contract/types.ts` | `AgentRequest` / `AgentResponse` (문자열만 반환 금지) |
| Feature flags | `agent/ops/featureFlags.ts` | `FeatureFlagDef` + `rolloutPercent` / `expiresAt` / `owner` |
| Prompt version | `agent/ops/promptVersion.ts` | 활성 `music-1.0.0` (semver) |
| Evaluation | `agent/ops/evaluation.ts` | confidence / hallucinationRisk / toolSuccess / **answerLength** |
| Cache | `agent/ops/cache.ts` | Question/Semantic 인터페이스(noop) — RAG 전 미구현 |
| Safety | `agent/ops/safety.ts` | Input/Output Guard(passthrough) — 미구현 |
| Memory | `agent/ops/memory.ts` | Summary Memory 훅 |
| Health | `agent/ops/health.ts` | HealthScore(Latency/Error/429/Success) + Circuit |
| Experiment | `agent/ops/experiment.ts` | A/B assign 훅 |
| Metrics | `agent/ops/metrics.ts` | answerLength·toolCalls·tokens |
| ReRank | `agent/context/rerank.ts` | `ReRankProvider` + `IdentityReRank` |
| Vector | `agent/context/vectorSearch.ts` | `VectorSearchResult` — 오늘은 `[]` |
| FAQ | `agent/context/faqKb.ts` | JSON-like KB + `faqMatch.ts` |
| Music | `MusicMetadataProvider` → Default `MelonProvider` | 앱: `nrmMusicMetadataProvider.ts` |

`AgentResponse`에 `ui.badges|actions|warnings`, `promptDiagnostics`, `contextDiagnostics` 포함.
앱은 `parseAgentUiFromDiag`로 말풍선 메타 표시.

### Music Tool — Melon 전용 (2026-07-24)

이번 버전 검색·다운로드는 **Melon만**.

```
search_music(query) → MelonProvider → searchMelonTracks
start_music_download(hit, lyricsOption)
  → Melon track detail 메타
  → YouTube 검색(artist + title from Melon)
  → 오디오 추출 + Melon 메타 임베드
  → (선택) 가사 생성
```

- AI Lab **플랫폼 선택** UI: 전체 플랫폼 목록 표시, **Melon만 선택 가능**(그 외 회색)
- 가용성: 토큰/연동 게이트 미사용 — `Melon=Enabled`, 그 외=`Disabled` 강제
- Spotify/Apple Music 등 요청 시: Function Call 없이 미지원 안내 + Melon 제안
- `lyricsOption`: `none|ko|en|auto|ko_translate|en_translate|auto_translate` (기본 `none`)
- 가사 옵션 없이 「가사도」만 요청 → FC 전 질문(한국어 팩/영어 팩/번역지원)

### FAQ Context

`FAQ_KB` 배열(keywords/answer) — Embedding 전환 용이. `matchFaqHits`는 일반 점수 루프만.

### 내일 RAG

`VectorSearchProvider.search()` → `ReRankProvider.rerank()` → Context Merge. 현재 vector는 빈 배열.

---

## AI Lab 채팅 전송 — Edge Function + Agent 파이프라인 (실제 구현)

`ApiKey`는 클라이언트(APK)에 절대 내려주지 않는다. 실제 LLM 호출은 Edge Function
`supabase/functions/llm-chat-send`(service_role)에서만 수행.

### Agent 파이프라인 (2026-07-24)

```
사용자 질문
  → nrm_rpc_chat_prepare_turn (세션/권한/쿼터/history≤15)
  → Intent Classifier (Gemini Flash Lite JSON)
       intent: general|music|latest|download|app|recommendation
       needSearch / needDownloadTools / needVector / needFaq
       (실패 시 heuristic_fallback)
  → Context Collectors (스텁)
       FAQ — faqData 키워드 매칭 / VectorDB — 스텁(내일 RAG)
  → toolMode = download | web_search | none  (배타)
  → 모듈형 System Prompt + 메인 LLM 스트리밍
  → finalize + waitUntil quota
```

모듈: `supabase/functions/llm-chat-send/agent/{types,intent,context,systemPrompt,plan}.ts`

| Intent 예 | toolMode | 비고 |
| --- | --- | --- |
| `BTS가 누구야` → music | none | plain LLM |
| `이번주 빌보드` → latest | web_search | Gemini `google_search` / Groq `browser_search` |
| `아이유 노래 넣어줘` → download | download | FC(앱 실행), 휴리스틱 정규식 제거 |
| `내가 좋아할 노래` → recommendation | none(+needVector) | Vector 스텁 — RAG 연동 시 Context 채움 |
| `로그인 안 돼요` → app | none(+needFaq) | FAQ 키워드 매칭 → FAQ_HITS |

**제거됨:** AI Lab UI 「인터넷」토글, 클라이언트 `enableWebSearch` 전송, `messageLikelyNeedsDownloadTools` 정규식.

**유지:** 권한/`AllocatedToken` 선체크, NDJSON, prepare/finalize RPC, rate_limit 안내, 다운로드 FC 클라이언트 루프.

1. **`nrm_rpc_chat_prepare_turn`** — 세션 확보/생성 → 사용자 메시지 저장 → `LLMModel⋈LLMProvider`·`LLMUserPermission`·`LLMUserQuota`·최근 대화이력(**최대 15건**, `system` 제외; 향후 Session Summary와 합칠 예정).
2. Edge가 **① IsActive 모델** → **② IsApproved** → **③ AllocatedToken** 선체크 후 Intent→메인 LLM. (DB `DailyLimit`/`MonthlyLimit` 선체크는 정규화로 제거됨 — 실제 하루 한도는 제공자 API 429.)
3. **`nrm_rpc_chat_finalize_turn`** — assistant/system 저장 + TokenHistory.
4. **`nrm_rpc_increment_llm_user_quota`** — `waitUntil` 백그라운드.

멀티 프로바이더: `ADAPTERS` + `PROVIDER_CAPABILITIES`(`supportsSearch`/`supportsTools`/`supportsVision`/`supportsThinking`). 현재 `Google`, `Groq`. OpenAI/Claude/xAI 등은 어댑터+캐파빌리티만 추가.

### 스트리밍 응답(타이핑 효과) — NDJSON

| `type` | 시점 | 내용 |
| --- | --- | --- |
| `meta` | prepare 직후 | sessionId / userMessage |
| `delta` | LLM 스트리밍 | text 조각 |
| `tool_request` | 다운로드 FC | callId/name/args — 앱이 실행 |
| `tool_turn_end` | tool_request 후 | 클라이언트가 toolContinue 재호출 |
| `final` | 확정 | assistant 또는 system |
| `error` | 복구 불가 | message |

도구 모드(Intent 배타):
- **download** → 다운로드 functionDeclarations만. LLM이 `list_ready_download_platforms` 등을 호출
- **web_search** → Gemini `google_search` / Groq `browser_search`(필요 시 gpt-oss-120b). 제공자 grounding이 검색→재작성
- **none** → tools 없음

클라이언트(`nrmLlmChatSend.ts`)는 `expo/fetch` NDJSON. 화면(`NrmDiscoverAiLabScreen`)은 검색 토글 없이 typing placeholder + markdown 렌더.

사전 체크 실패·`adapter_missing`도 동일 NDJSON(`delta` 없이 `final`). prepare 자체 실패만 JSON 4xx/5xx.

**참고(구버전):** 아래 문단의 DailyLimit 선체크·「많은 사용자」문구·history 40·UI 검색 토글은 **현재 코드와 불일치**할 수 있다. Edge `index.ts` + `agent/` 를 SSOT로 본다.

---

### (이력) 이전 서술 — DailyLimit / 토글 시대

`ApiKey`는 클라이언트에 내려주지 않는다. 과거 구현 요약:

1. **`nrm_rpc_chat_prepare_turn`** (service_role 전용, `20260721130000_chat_send_rpc.sql` → 한도 체크 필드 추가는 `20260721140000_chat_provider_limit_check.sql`) — DB 왕복 1회로:
   세션 확보/생성 → 사용자 메시지 저장 → `LLMProvider`(ApiKey 포함, `DailyLimit`/`MonthlyLimit`과 그 기준 **전체 사용자 합산** 현재 사용량 `providerDailyUsed`/`providerMonthlyUsed`도 함께)·`LLMUserPermission`·해당월 `LLMUserQuota`·최근 대화이력(최대 40건, `system` 제외)을 한 번에 조회해 반환.
   - `providerDailyUsed` = 오늘(UTC) `LLMTokenHistory."TotalToken"` 합계(해당 `ProviderID`, 전체 사용자). `DailyLimit=0`이면 이 집계 자체를 건너뛴다(가장 흔한 "무제한" 케이스에서 추가 비용 없음).
   - `providerMonthlyUsed` = 이번 달 `LLMUserQuota."TotalToken"` 합계(해당 `ProviderID`, 전체 사용자). `MonthlyLimit=0`이면 건너뛴다.
2. Edge Function이 위 결과로 **① 제공자(모델) 단위 `DailyLimit`/`MonthlyLimit`** → **② 사용자 권한(`IsApproved`)** → **③ 사용자 개인 쿼터(`AllocatedToken`)** 순으로 선체크 후, 모두 통과하면 프로바이더 어댑터(`ProviderName` 기준: `Google`→Gemini, `Groq`→OpenAI호환 `chat/completions`)로 실제 LLM API를 호출. Gemini `generateContent` 응답의 `usageMetadata`(`promptTokenCount`/`candidatesTokenCount`/`totalTokenCount`)를 그대로 써서 **토큰 집계용 별도 API 호출이 필요 없다.** Groq는 `usage.prompt_tokens`/`completion_tokens`/`total_tokens`를 사용한다.
3. **`nrm_rpc_chat_finalize_turn`** (service_role 전용) — DB 왕복 1회로: 응답(`assistant`/`system`) 메시지 저장 + `ChatSession.UpdateDate` 갱신 + (실제로 AI 요청을 시도한 경우만) `LLMTokenHistory` 기록.
4. **`nrm_rpc_increment_llm_user_quota`** (service_role 전용) — `LLMUserQuota` 누적 upsert(`ON CONFLICT` 원자적 증가). 사용자 응답을 이미 반환한 **뒤** `EdgeRuntime.waitUntil()`로 백그라운드 실행 — 응답 지연에 전혀 영향 없음.

사전 체크 실패(제공자 한도 소진/권한 없음/쿼터 초과)는 AI 요청 자체를 하지 않으므로 `LLMTokenHistory`에 기록하지 않는다(요구사항). 네트워크 오류·프로바이더 오류처럼 **AI 요청을 시도한** 경우는 실패해도 `isSuccess=false`로 기록한다.

**컬럼명 정정 (2026-07-22)**: 최초 추가(`20260721150000`) 시 `isSucess`(오타)로 만들어졌던 것을 `20260722100000_llm_token_history_is_success_rename.sql`에서 `RENAME COLUMN`으로 `isSuccess`로 정정했다(데이터 보존). `nrm_rpc_chat_finalize_turn`도 같은 마이그레이션에서 `isSuccess` 컬럼을 쓰도록 `CREATE OR REPLACE`. 과거 마이그레이션 파일(`20260721130000`/`20260721140000`/`20260721150000`) 자체는 이미 원격에 적용된 이력이라 수정하지 않고 그대로 둔다(신규 마이그레이션으로만 교정 — 이 저장소 관례).

**"토큰이 만료되었을 경우" 메시지의 의미 구분** (설계 결정, 2026-07-21):
- 개인 할당량(`LLMUserPermission.AllocatedToken`) 소진 → `토큰이 만료되었어요. 관리자에게 문의해주세요.` (관리자가 늘려주면 바로 해결됨)
- 모델(`LLMProvider`) 자체의 `DailyLimit`/`MonthlyLimit`(전체 사용자 합산) 소진 → `지금 이 모델을 많은 사용자가 이용하고 있어 잠시 이용이 어려워요. 나중에 다시 시도해 주세요.` (관리자 문의로 즉시 해결되는 성격이 아니므로 문구를 구분)
- Gemini API 자체의 인증 오류(401/403) → `네트워크 문제로 요청할 수 없어요...` (앱에 실사용자 로그인 토큰이 없어 "토큰"이라는 표현은 위 두 가지 앱 내부 토큰 할당량 개념에만 사용)

멀티 프로바이더 확장: Edge Function 안 `ADAPTERS: Record<ProviderName, LlmAdapter>` 맵에 새 어댑터를 추가하고 `LLMProvider.ProviderName`을 맞추면 나머지 로직(권한/쿼터/기록/세션)은 그대로 재사용된다. 현재 등록: `Google`, `Groq`.

**Groq 요청 예산 (2026-07-23):** 질문 크기·`max_tokens` 예약 때문에 요청을 거절하지 않는다. `prompt+max_tokens`가 모델 TPM 요청 상한을 넘지 않도록 `max_tokens`를 잡되, **선호 상한 3072**로 한 턴이 TPM 전체를 쓰지 않게 해 같은 분 안 연속 질문이 바로 429 나지 않게 한다. 부족하면 **과거 대화만** 줄인다. 429에 `Used/Limit/Requested`가 있으면 잔여 TPM에 맞춰 `max_tokens`를 줄여 **즉시 1~2회 재시도**. `finish_reason=length`면 이어쓰기 1회. 모델이 content에 넣은 `<think>` 등 내부 추론은 전송 전 제거. 분당 TPM이 실제로 바닥나거나(재시도로도 불가) RPM/RPD·관리자 `AllocatedToken` 초과만 다음 요청을 정책적으로 막는다.

### 스트리밍 응답(타이핑 효과) — NDJSON (2026-07-22)

사전 체크(제공자 한도/권한/쿼터)를 통과하면 Edge Function은 Gemini를 호출해 응답을 받는다(업스트림 호출 방식은 아래 "후속 수정 2" 참고 — 2026-07-22부터 논스트리밍 `generateContent` + 서버 재생으로 바뀌었다). Edge Function → 클라이언트 구간은 이 업스트림 방식과 무관하게 항상 "타이핑 효과"용 **NDJSON**(줄바꿈으로 구분된 JSON) 스트림이다(`Content-Type: application/x-ndjson`):

| `type` | 시점 | 내용 |
| --- | --- | --- |
| `meta` | `prepare_turn` 완료 즉시(권한/한도 체크 **전**). `toolContinue` 시에도 세션 메타만 재전송 | `sessionId`/`isNewSession`/`title`(임시)/`userMessage` — 클라이언트가 사용자 메시지·세션을 바로 확정 표시 |
| `delta` | LLM 스트리밍 중 0회 이상 | `text`(조각) — 어시스턴트 말풍선에 이어붙여 타이핑 효과 |
| `tool_request` | Gemini가 functionCall을 반환한 경우 1회 이상 | `callId`/`name`/`args` — 클라이언트가 앱 로컬 도구(다운로드 플랫폼·검색·가사·시작)를 실행 |
| `tool_turn_end` | `tool_request` 직후 스트림 종료 | `sessionId`/`partialText` — 클라이언트가 tool 실행 후 `toolContinue`+`toolResults`로 같은 세션을 재호출 |
| `final` | 이번 턴 확정 메시지 저장 완료 후 1회, 스트림 종료 | `message`(assistant 또는 system 최종 `ChatMessage` row) + optional `choices`(플랫폼/트랙/가사 칩) — `delta`로 이미 보여준 텍스트와 항상 일치 |
| `error` | 복구 불가 오류(예: finalize 저장 실패) | `message` — 이 경우 `final` 없이 스트림이 끝날 수 있음 |

**이력:** 과거에는 다운로드 정규식 휴리스틱과 UI 검색 토글이 tool 모드를 결정했다. 2026-07-24부터 Intent Classifier가 대체한다.

도구 실행:
- 다운로드 FC → **앱 클라이언트** (`tool_request` / `toolContinue`)
- 인터넷 검색 → **각 LLM 제공자 네이티브 검색**(Edge 크롤 없음). grounding이 검색 결과를 LLM이 재정리한 답으로 스트리밍.

사전 체크 실패·`adapter_missing`처럼 LLM을 호출하지 않는 경로도 **동일한 스트림 프로토콜**을 쓴다(다만 `delta` 없이 곧바로 `final`). `prepare_turn` 자체가 실패한 경우(파라미터 오류 등, 세션도 아직 없음)만 스트리밍 없이 평범한 JSON 에러 응답을 즉시 반환한다.

클라이언트(`app/lib/nrmLlmChatSend.ts`)는 `@supabase/supabase-js`의 `functions.invoke()`(RN 기본 fetch가 응답을 통째로 버퍼링해 스트리밍이 안 됨) 대신 **`expo/fetch`**(Expo SDK 50+ 네이티브 스트리밍 fetch, `response.body`가 진짜 `ReadableStream`)로 Edge Function URL(`getNrmSupabaseFunctionUrl()`)을 직접 호출한다. Authorization/apikey 헤더는 publishable key를 그대로 사용(`functions.invoke()`와 동일한 인증 방식).

클라이언트 화면(`NrmDiscoverAiLabScreen.tsx`)은 전송 즉시(첫 `delta` 도착 전) "타이핑 중" 점 3개 애니메이션이 있는 빈 어시스턴트 버블을 먼저 붙인다(`NrmAiLabMessage.typing`, `NrmAiLabTypingDots.tsx`). **인터넷 검색 토글 UI는 제거**되었다.

사용자가 좌측 메뉴에서 직접 고른 모델(`LLMModel.ModelID`)은 `app/lib/nrmAiLabModelPreference.ts`가 `AsyncStorage`에 저장한다. 앱 재시작 후에도 그 값을 복원하며, 피커의 기본값 자동 선택은 **저장을 덮어쓰지 않는다**. 과거 대화를 열어도 피커 선택은 바꾸지 않고, `llm-chat-send`/`nrm_rpc_chat_prepare_turn`은 **요청 body의 `modelId`를 우선**해 호출한다(기존 세션이면 `ChatSession.ModelID`도 같이 갱신).

#### 알려진 이슈 — Gemini "thinking"이 강제인 모델의 지연·잘림 (2026-07-22)

`gemini-flash-latest`(2026-07-22 기준 `gemini-3.5-flash`로 alias, `docs` 위 문단 참고)로 실제 로그(`llm_call_ok`)를 확인한 결과:

- 매 요청마다 `usageMetadata.thoughtsTokenCount`가 ~1,100~1,200 정도 소모되고, 그 뒤 시각적으로 보이는 답변은 극히 짧게(문장 중간에서도) 끝난 채 스트림이 종료된다. `candidates[0].finishReason`이 **한 번도 오지 않음**(로그에 `finishReason` 필드 자체가 없음) — 모델이 스스로 끝낸 게 아니라 연결이 외부에서 끊긴 것으로 보인다.
- `generationConfig.thinkingConfig.thinkingBudget`를 `0`으로 주면 이 모델은 **400 Invalid Argument**로 거부한다. `4096`으로 줘도(값 자체는 받아들여지는 것으로 보임) 총 사용 토큰·지연·잘림 패턴이 동일했다 — thinking 예산을 우리가 조절해서 고칠 수 있는 문제가 아니었다.
- `generationConfig.maxOutputTokens`를 넉넉히(`8192`, `GEMINI_CHAT_MAX_OUTPUT_TOKENS`) 올려도 총 사용 토큰이 그 한도에 한참 못 미치는 상태(~1,150~1,240)에서 끊겨, 우리 쪽 토큰 상한이 원인이 아님을 확인했다.
- 결론: 이 특정 alias(당시 최신 preview일 가능성이 높은 모델)의 서버 쪽 지연/자원 제약으로 보이며, 코드로 해결 불가능. **더 빠르고 완전한 답변이 필요하면 `IsActive`를 thinking이 없는 안정 모델(예: `models/gemini-2.0-flash`) 또는 확실히 지원되는 `models/gemini-2.5-flash` 계열로 옮기는 것을 권장** — `getGeminiThinkingConfig()`가 `2.0` 계열엔 `thinkingConfig`를 아예 안 보내므로 그런 모델은 이 문제와 무관하다.
- 그래도 코드에서 해둔 안전장치: `stream()`/`generateTitle()` 모두 `thinkingConfig`가 400으로 거부되면 그 필드만 빼고 1회 자동 재시도(모델이 바뀌어도 하드코딩 없이 대응). `llm_call_ok` 로그에 `finishReason`/`thoughtsTokens`를 항상 남겨 다음에 같은 증상이 나오면 바로 판단 가능.

#### 후속 수정 1 — maxOutputTokens를 모델별 실제 한도까지 올림 (2026-07-22, 부분적 개선)

`models/gemini-flash-lite-latest`(위 이슈의 alias와 별개로, 실제로 응답이 오는 모델)에서도 "답변이 중간에 잘리고 말이 이상해진다"는 리포트가 있어, 우선 `GEMINI_CHAT_MAX_OUTPUT_TOKENS`(전 모델 공통 `8192`)를 모델 등급별로 올렸다 — Gemini 2.5/3.x(및 `-latest` alias) 계열의 실제 output token 한도는 **65536**(2.0 계열만 8192). `getGeminiMaxOutputTokens(modelName)`가 `getGeminiThinkingConfig()`와 같은 기준으로 `GEMINI_CHAT_MAX_OUTPUT_TOKENS_MODERN=65536`/`..._LEGACY=8192`를 고른다. 답변 품질(자연스러움) 개선을 위해 `generationConfig.temperature=0.8`도 명시(이전엔 모델 기본값 사용).

이 수정 후에도 실제 로그로 재현한 결과 증상이 그대로였다 — 아래 "후속 수정 2"에서 진짜 원인을 확인.

#### 후속 수정 3 — 실시간 SSE 스트리밍 복구 + 검색 인용 정리 (2026-07-24)

논스트리밍 `generateContent` + `emitAsTypingDeltas` 재생은 LLM 완료 전까지 첫 글자가 오지 않아 "한 번에 그려지는" 체감이 났다. **다시 제공자 SSE를 릴레이**한다.

- Gemini: `streamGenerateContent?alt=sse`. `finishReason` 없이 조기 종료되면 같은 요청을 논스트리밍 `generateContent`로 한 번 더 받아 확정(과거 SSE 이슈 안전장치).
- Groq: `stream: true` + OpenAI SSE. 검색 시 `citation_options: disabled`로 출처/인라인 인용을 요청하지 않음. 본문에 남는 `【n†…】`는 sanitize로 제거.
- 출처 UI·파싱·저장 없음. Gemini `google_search`는 검색 자체에 grounding 메타가 따라오지만 Edge는 URI 목록을 파싱·전달하지 않는다.

#### 후속 수정 2 — chat도 스트리밍(SSE) 대신 논스트리밍 `generateContent` + 서버 재생으로 전환 (2026-07-22, 근본 수정)

`llm_stream_debug_chunk`/`llm_stream_debug_end` 임시 로그로 `models/gemini-flash-lite-latest` 호출을 직접 재현해 확인한 실제 원인:

- `streamGenerateContent?alt=sse` 응답이 **SSE 프레임 1~2개만 온 뒤 `finishReason` 없이 연결이 그대로 끝났다**(`reader.read()`가 추가 데이터 없이 `done:true`를 반환). `maxOutputTokens`를 65536까지 올려도 무관 — 실제 사용 토큰(`usageMetadata`)이 그 한도에 한참 못 미친 상태(2~13 output tokens)에서 끊겼고, thinking 예산(`thoughtsTokenCount`)도 `0`이었다. 즉 **우리 쪽 토큰 상한·thinking 설정이 원인이 아니라, Google SSE 게이트웨이가 완료 신호(`finishReason`) 없이 스트림을 조기 종료**하는 문제였다(재현율 100%, 메시지 내용과 무관).
- `generateTitle()`은 원래부터 논스트리밍 `generateContent`를 쓰고 있었고 이런 조기 종료 증상이 보고된 적이 없어, **chat(`stream()`) 호출도 동일하게 `generateContent`(논스트리밍)로 교체**했다. 논스트리밍은 응답이 불완전하면 HTTP body/JSON 파싱 자체가 실패하므로(예외 발생) "일부만 온 응답을 완성된 것으로 오인"할 수가 없다 — SSE 프레이밍 특유의 이 취약점을 구조적으로 제거.
- 실시간 네트워크 스트리밍을 포기하는 대신, 응답을 통째로 받아 확정한 뒤 **서버(`emitAsTypingDeltas`)가 `onDelta`를 짧은 간격(16자/30ms)으로 여러 번 호출해 타이핑 효과를 재생**한다. 클라이언트가 받는 NDJSON 프로토콜(`meta`/`delta`/`final`)은 그대로이므로 앱 쪽 코드는 변경 불필요. 실제 네트워크 호출 자체는 1~5초로 이미 빠르고, 전송 즉시 붙는 "타이핑 중" 표시(typing placeholder) 덕분에 체감 차이가 없다.
- 수정 후 실제 테스트(자기소개 5문장/피보나치 함수/전통음식 3가지 소개 등 이전에 2~13 토큰에서 끊겼던 프롬프트들)에서 149~616 output tokens의 완전하고 자연스러운 답변이 생성됨을 확인.
- 위 "알려진 이슈"(`gemini-flash-latest`)의 근본 원인도 이 문서 작성 시점 기준 사실상 동일한 SSE 조기 종료 계열 문제였을 가능성이 높다(다만 그쪽은 thinking 토큰이 커서 시간이 더 걸렸을 뿐) — 이번 수정으로 `stream()`을 쓰는 모든 모델이 함께 개선된다.

### 대화 제목 (휴리스틱 전용, 2026-07-23 변경)

`nrm_rpc_chat_prepare_turn`이 새 세션 생성 시 "사용자 메시지를 28자로 자른" 제목을 즉시 넣는다. **추가 Gemini 호출로 제목을 다시 쓰지 않는다**(free-tier `generateContent` 요청 횟수를 채팅 본문과 나눠 쓰지 않기 위함). 예전(2026-07-22)에는 응답 후 백그라운드에서 제목용 `generateContent` 1회를 더 호출했다.

### Gemini 호출 횟수 최적화 (2026-07-23)

채팅 1턴당 Edge→LLM 본문 호출은 **원칙적으로 1회**(다운로드 toolContinue 제외).

- Intent `needSearch`: Gemini `google_search` / Groq `browser_search` — 제공자 서버사이드 검색 포함 **1회**
- Intent `needDownloadTools`: 다운로드 FC — 도구 턴마다 클라이언트가 Edge를 다시 부르므로 그때만 추가 1회
- 429: 대기 재시도 없이 즉시 `rate_limit` 반환
- 대화 history: 최근 **15**턴 (향후 Summary 메모리 확장 포인트)
- Intent 분류: Google `gemini-2.0-flash-lite` 별도 1회(실패 시 휴리스틱, 메인 쿼터와 분리 가능)

**현재 활성 LLM 모델 (2026-07-23 기준)**: Groq(`ProviderID=2`) 활성 — `openai/gpt-oss-120b`(`ModelID=1002`), `qwen/qwen3.6-27b`(`1001`), `llama-3.3-70b-versatile`(`1000`)이 피커 상단(ModelID DESC). Google `Type='LLM'` 활성 — `models/gemini-3.1-flash-lite`(`21`), `models/gemini-3.5-flash`(`28`), `models/gemini-3.5-flash-lite`(`55`), `models/gemini-3.6-flash`(`56`). Groq 시드: `20260723190000_llm_provider_groq.sql`. 2026-07-23 Gemini 목록 재조회 시 기존 행은 유지하고 없던 `models/gemini-3.5-flash-lite`·`models/gemini-3.6-flash`만 추가한 뒤(`20260723090000_llm_model_append_gemini36.sql`), `gemini-3.5-flash-lite`도 활성화했다(`20260723093000_llm_model_activate_gemini35_flash_lite.sql`). `Type<>'LLM'`(Embedding/TTS/Image/Video)은 이 변경과 무관하게 그대로 둔다. `admin` SerialNo는 전체 `ProviderID`에 대해 `LLMUserPermission`(`IsApproved=true`, 무제한)을 갖는다(`scripts/seed-llm-admin-permissions-all.mjs`, Groq는 마이그레이션에서 `ProviderID=2` 권한도 INSERT). `LLMModel.IsActive=false`면 `llm-chat-send`가 `provider_unavailable`로 막는다.

### 로깅 — 서버(Edge Function) + 앱(client)

**서버(`supabase/functions/llm-chat-send/index.ts`)**: 요청마다 `requestId`(UUID)를 하나 생성해 모든 로그 줄에 붙인다. Supabase Dashboard → Edge Functions → `llm-chat-send` → Logs (또는 `supabase functions logs llm-chat-send`)에서 `requestId`로 grep하면 그 요청의 전체 흐름을 순서대로 재구성할 수 있다. 단계별 이벤트:

| event | 의미 |
| --- | --- |
| `request_received` | 파라미터 검증 통과 직후 (serialNo/providerId/sessionId/messageLength/미리보기) |
| `invalid_json` / `invalid_params` / `method_not_allowed` / `server_misconfigured` | 요청 자체 문제 |
| `prepare_turn_ok` / `prepare_turn_failed` | 1번 RPC 결과 + 소요시간(`elapsedMs`) + provider(dailyLimit/monthlyLimit/사용량 포함)/permission/quota 요약 |
| `provider_unavailable` | `LLMProvider`가 없거나 `IsActive=false` |
| `provider_daily_limit_exceeded` / `provider_monthly_limit_exceeded` | 모델 자체의 `DailyLimit`/`MonthlyLimit`(전체 사용자 합산) 소진 (AI 요청 자체를 하지 않음) |
| `permission_denied` | `LLMUserPermission` 미승인 (AI 요청 자체를 하지 않음) |
| `quota_exceeded` | 개인 `AllocatedToken` 초과 (AI 요청 자체를 하지 않음) |
| `adapter_missing` | `ProviderName`에 대응하는 어댑터 미구현 |
| `llm_call_start` / `llm_call_ok` / `llm_call_failed` | 실제 LLM API 호출 — `elapsedMs`, 토큰 수, 실패 `kind`(`auth`/`network`/`rate_limit`/`other`) + HTTP status. **429 RESOURCE_EXHAUSTED**(free tier 한도 등)는 `rate_limit` |
| `gemini_attempt_failed` / `gemini_attempt_ok` | 시도별(채팅 턴당 원칙 1회). 429는 재시도 없이 즉시 `rate_limit` |
| `attempt_log_insert_ok` / `attempt_log_insert_failed` | `LLMCallAttemptLog` INSERT 결과 |
| `finalize_turn_ok` / `finalize_turn_failed` | 2번 RPC 결과 |
| `quota_increment_ok` / `quota_increment_failed` / `quota_increment_threw` | 백그라운드(`waitUntil`) 쿼터 누적 결과 — 응답 반환 **후** 로그이므로 클라이언트 응답 시점보다 늦게 찍힐 수 있음(정상) |
| `title_generate_skipped` | 새 세션에서도 LLM 제목 생성 생략(휴리스틱 유지, 쿼터 절약) |
| `stream_cancelled_by_client` | 클라이언트가 스트림 읽기를 중단(앱 종료·화면 이탈 등) — `ReadableStream.cancel` |
| `stream_unhandled_error` | 스트림 `start()` 콜백 내부에서 위 어디에도 안 걸린 예외 — `error` 이벤트 전송 후 스트림 종료 |
| `request_done` | 요청 종료 요약 1줄 — `outcome`(`success`/`provider_daily_limit_exceeded`/`provider_monthly_limit_exceeded`/`permission_denied`/`quota_exceeded`/`llm_call_failed`/`provider_unavailable`/`adapter_missing`/`finalize_failed`) + `totalElapsedMs`. 세부 값은 위 단계별 로그에만 있고 여기서는 반복하지 않음(중복 방지) |
| `unhandled_error` | 위 어디에도 안 걸린 예외(스트림 시작 전) — 항상 최상위 `try/catch`로 포착되어 500으로 응답 |

ApiKey와 대화 전문은 로그에 남기지 않는다(메시지·응답은 60자 미리보기만, 길이는 전체).

**앱(`app/lib/nrmLlmChatSend.ts`, tag=`ailab.llmSend`)**: `start` → `final`(role/contentPreview/`diag`) → `ok`, 또는 `fetch_error`/`server_error`/`stream_error`/`no_final`.  
`final.diag`에는 서버가 넣은 `outcome`/`attempts`/`lastError`가 그대로 온다. `RequestID`로 `LLMCallAttemptLog`와 조인 가능.

### 사용량 조회 화면 — AI Lab 좌측 메뉴 (2026-07-22)

AI Lab 좌측 메뉴의 「사용량 조회」는 모델(`LLMProvider`)별 월간 할당 토큰(`LLMUserPermission.AllocatedToken`)과 그 달 사용 토큰(`LLMUserQuota.TotalToken`)을 도넛 차트로 보여준다. 서버 RPC 없이 클라이언트가 두 테이블을 직접 SELECT한다(2026-07-21 보안 강화 마이그레이션에서 이미 anon/authenticated에 `SELECT ... USING (true)`를 허용해뒀고, `ApiKey`만 컬럼 단위로 막혀 있어 이 두 테이블은 애초에 막을 컬럼이 없다).

- **`app/lib/nrmLlmUsageClient.ts`**
  - `fetchLlmUsageProviderOptions(serialNo)` — `LLMUserPermission`에서 `SerialNo` + `IsApproved=true`인 행만 가져와 `LLMProvider`(캐시된 `fetchLlmProvidersForAiLab()`) 목록과 조인해 모델 선택 드롭다운 항목을 만든다. `LLMProvider.ApiKey`는 원래 이 목록에 없다(공개 컬럼만 select).
  - `fetchLlmUsageMonthSnapshot(serialNo, providerId, targetMonth)` — 특정 모델 × 특정월(YYYYMM)의 `LLMUserPermission`(1건, `maybeSingle`) + `LLMUserQuota`(1건, UNIQUE(`SerialNo`,`ProviderID`,`TargetMonth`)라 항상 0~1건)를 병렬 조회해 합친다. 해당 월 사용 기록이 없으면(`LLMUserQuota` 행 없음) `usedToken=0`으로 취급 — "그 달 아직 안 썼음"과 동일하므로 별도 처리가 필요 없다.
  - `isNrmLlmAllocationUnlimited(allocatedToken)` — `AllocatedToken === 0`(`NRM_LLM_UNLIMITED_ALLOCATED_TOKEN`) 여부만 판별하는 얇은 헬퍼(기존 규칙 재사용).
- **`app/lib/nrmLlmUsageMonth.ts`** — `LLMUserQuota.TargetMonth`(`YYYYMM`) 전용 월 계산(`nrmCurrentTargetMonth`/`nrmShiftTargetMonth`/`nrmIsFutureTargetMonth`/`nrmFormatTargetMonthLabel`). 미래 월로는 이동 불가(다음 달 버튼 비활성)하되, 과거로는 데이터가 없어도(당시 `LLMUserQuota` 행이 없으면 0으로 표시) 자유롭게 이동 가능.
- **`app/components/nrm/discover/NrmAiLabUsageDonut.tsx`** — `react-native-svg`의 `Circle` + `Animated.createAnimatedComponent`로 만든 순수 도넛 컴포넌트. 모델/월이 바뀔 때마다 `strokeDashoffset`을 0%에서 목표값까지 다시 애니메이션(`Animated.timing`, 900ms, ease-out)해 "스르륵" 채워지는 효과를 낸다.
- **`app/components/nrm/discover/NrmAiLabUsagePanel.tsx`** — 실제 화면(모델 선택 트리거 + 월 이동 화살표 + 도넛 + 사용/할당/남은 토큰 통계). `AllocatedToken=0`(무제한)이면 퍼센트 개념이 없으므로 도넛을 항상 100%(`success` 색)로 채우고 중앙에 인피니티 아이콘 + "무제한"을 표시, 하단 통계에서 "남은 토큰" 행은 숨긴다. 그 모델에 대해 `LLMUserPermission` 행 자체가 없거나 `IsApproved=false`면("사용 권한 없음") 도넛 대신 안내 문구만 보여준다.
- **`app/components/nrm/discover/NrmAiLabSidebar.tsx`** — 「사용량 조회」패널(`panel==='usage'`)에서 위 `NrmAiLabUsagePanel`을 렌더링. 기본 선택 모델은 AI Lab에서 현재 쓰고 있는 모델(`llmProviderId` prop, 승인 목록에 있을 때만)이고, 없으면 승인된 첫 모델로 대체한다. `serialNo`(`app/lib/nrmLlmSerialNo.ts` 규칙과 동일한 문자열)는 `NrmDiscoverAiLabScreen.tsx`가 이미 갖고 있는 값을 그대로 prop으로 내려받아 재조회하지 않는다.

## LLMUserMonthlyAllocation

관리자페이지 「AI토큰 할당」의 월별(YYYYMM) 설정 이력. **실시간 사용 한도 체크에는 관여하지 않는다** — 그 역할은 여전히 `LLMUserPermission.AllocatedToken`(항상 "현재 적용 중인 값")이 담당한다. 이 테이블은 "그 값이 언제·얼마로 설정됐는지" 조회용 이력일 뿐이다.

| 컬럼 | 타입 | 기본값 | NULL | 설명 |
|------|------|--------|------|------|
| `AllocationID` | `bigint` IDENTITY | — | NO | 할당 이력 고유번호 **(복합 PK)** |
| `SerialNo` | `varchar` | — | NO | 사용자 일련번호 **(복합 PK)** |
| `ProviderID` | `bigint` | — | NO | LLM 제공자 고유번호 |
| `TargetMonth` | `varchar(6)` | — | NO | 할당 대상월 (`YYYYMM`) |
| `AllocatedToken` | `bigint` | `0` | NO | 해당 월에 설정된 할당 토큰 (`0` = 무제한) |
| `UpdatedBySerialNo` | `varchar` | — | YES | 설정을 실행한 관리자 SerialNo (항상 `admin`, 감사용) |
| `RegDate` | `timestamptz` | `now()` | NO | 최초 생성일시 |
| `UpdateDate` | `timestamptz` | `now()` | NO | 최종 수정일시 |

- **PK** `PK_LLMUserMonthlyAllocation` (`AllocationID`, `SerialNo`)
- **UNIQUE** `UX_LLMUserMonthlyAllocation` (`SerialNo`, `ProviderID`, `TargetMonth`) → 동일 사용자·제공자·월은 1건만 존재(그 달의 마지막 설정값만 보관, 수정 이력 자체는 남기지 않음).
- RLS: `SELECT`만 anon/authenticated 허용(`USING (true)`), 쓰기는 아래 RPC 전용.

### 관리자 RPC — `nrm_rpc_admin_set_llm_user_token_allocation`

`supabase/migrations/20260722140000_llm_user_monthly_allocation.sql`. 파라미터: `p_caller_serial`(admin 검증용), `p_serial_no`, `p_provider_id`, `p_target_month`(YYYYMM), `p_allocated_token`, `p_is_approved`. 한 트랜잭션에서:

1. `LLMUserPermission`(`SerialNo`,`ProviderID`) upsert — `IsApproved`/`AllocatedToken`/`ApprovedDate`/`UpdateDate` 갱신(**실시간 적용값**).
2. `LLMUserMonthlyAllocation`(`SerialNo`,`ProviderID`,`TargetMonth`) upsert — 그 달 설정값 이력 기록.

`nrm_is_admin_caller`로 `p_caller_serial === 'admin'`이 아니면 `admin required` 예외. `anon`/`authenticated`에 `GRANT EXECUTE`(APK publishable key로 호출, 관리자 세션 게이트는 앱 UI 단 `nrmAdminAuth`가 담당 — `구현 시 참고` 항목 참고).

### 관리자페이지 UI (2026-07-22)

앱 메뉴 → 관리자페이지 → **Discover 편집** 아래에 두 항목 추가:

- **「AI토큰 조회」** (`app/components/nrm/settings/NrmAdminLlmTokenLookupPanel.tsx`) — 「전체」/「사용자별」 탭.
  - 전체: 제공자(모델) 선택 + 월 이동 → 그 달 **전체 사용자 합산** 사용량(`LLMUserQuota.TotalToken` 합계, `fetchProviderAggregateMonthUsage`)과 `LLMProvider.MonthlyLimit`(0=무제한)을 도넛으로 표시. "사용한 토큰량"은 사용자별이 아니라 **그 모델 자체**의 전체 사용량이라는 요구사항 그대로.
  - 사용자별: 사용자 선택(`NrmAdminUserPickerModal` 재사용) + 제공자 선택 + 월 이동 → `fetchLlmUsageMonthSnapshot(serialNo, providerId, targetMonth)`(AI Lab 사용량 화면과 동일 함수, 재사용)로 그 사용자의 할당 대비 사용량을 도넛으로 표시. 승인되지 않았거나 권한 행이 없으면 AI Lab 사용량 화면과 동일하게 안내 문구만 표시.
- **「AI토큰 할당」** (`app/components/nrm/settings/NrmAdminLlmTokenAllocationPanel.tsx`) — 사용자 선택 + 제공자 선택 + 월 이동.
  - **이번 달**: `LLMUserPermission`의 실시간 값을 불러와 편집 폼(할당 토큰 입력 + 승인 스위치)으로 표시, 저장 시 `nrm_rpc_admin_set_llm_user_token_allocation` 호출.
  - **과거 달**: 편집 불가(요구사항 — "현재 월에서는 토큰을 부여하거나 수정할 수 있어야해"). `LLMUserMonthlyAllocation`에서 그 달 이력을 조회해 읽기 전용으로 표시(이 기능 도입 이전 달은 기록이 없으므로 "기록이 없습니다" 표시).

공통 컴포넌트: `NrmAdminMonthNavRow.tsx`(월 이동 화살표), `NrmAdminLlmProviderPickerModal.tsx`(전체 제공자 선택 팝업 — AI Lab 사용량 화면의 승인된 목록만 보여주는 피커와 달리 Type 무관 전체 제공자를 보여준다), `app/lib/nrmLlmAdminTokenClient.ts`(`fetchAllLlmProvidersForAdmin`/`fetchProviderAggregateMonthUsage`/`fetchLlmUserMonthlyAllocationRecord`/`setLlmUserTokenAllocation`).

## 구현 시 참고

- 호출 전: `LLMUserPermission`에서 `IsApproved`·`AllocatedToken`을 확인한다.
- **`AllocatedToken = 0`** → 할당 한도 없음(무제한). (`LLMProvider`.`DailyLimit`/`MonthlyLimit`의 `0`과 동일 규칙)
- `SerialNo`(`LLMUserQuota`/`LLMTokenHistory`/`LLMUserPermission`)는 **varchar** — 앱 `getNrmAppSerialNo()` 원문을 형 변환 없이 그대로 저장한다. 앱: `app/lib/nrmLlmSerialNo.ts`
  - **admin APK**: text `"admin"` 그대로 저장 (과거엔 `bigint`여서 `0`으로 매핑했었으나 varchar 전환 후 더 이상 변환하지 않음)
  - **커스텀 APK**: numeric serial도 문자열 그대로 저장 (앞자리 `0` 보존, 예: `"01092452918"`)
- 호출 후: `LLMTokenHistory` INSERT + 해당 월 `LLMUserQuota` upsert(누적)를 함께 갱신하는 흐름을 권장.
- **복합 PK**: `LLMUserPermission` (`PermissionID`, `SerialNo`), `LLMUserQuota` (`QuotaID`, `SerialNo`), `LLMTokenHistory` (`HistoryID`, `SerialNo`). PostgREST upsert·앱 코드에서 `onConflict` 대상 컬럼을 복합 PK에 맞출 것.
- `DailyLimit` / `MonthlyLimit`는 **제공자** 단위 제한, `AllocatedToken`은 **사용자×제공자** 할당 한도다.
- 대화 세션·메시지는 [`chat.md`](./chat.md) (`ChatSession`, `ChatMessage`).
- FK(`ProviderID` → `LLMProvider`)는 현재 DDL에 없음. 필요 시 마이그레이션으로 추가 가능.
- 대화 제목은 `nrm_rpc_chat_update_session_title`(service_role 전용)로만 갱신된다 — 위 "대화 제목 자동 생성" 참고.
