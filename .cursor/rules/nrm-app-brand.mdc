---
description: 앱 브랜드(NullReference Music 고정) · 커스텀 APK userName / legacy appName
globs:
  - app/nrm-brand.config.json
  - app/lib/nrmAppBrand.ts
  - app/lib/nrmUserDisplayNameSettings.ts
  - app/scripts/sync-nrm-brand.mjs
alwaysApply: false
---

# 앱 브랜드 · 사용자 이름

## 제품명 (고정)

앱 내 로고·약관·종료 확인·런처 상호는 항상 **NullReference Music** (`versionInfoProductName` / `getNrmProductDisplayName()`).

커스텀 빌드의 **appName은 branding에 쓰지 않는다.** `user_list.app_name` legacy 기록용만.

## userName

- bake → 문의 `p_user_name`, 버전 `Custom : …`, user_list
- 메뉴 **사용자 이름 변경** 기본/초기화값
- 설정 오버라이드는 **AI Lab `~~님 안녕하세요`만** (`nrmUserDisplayNameSettings.ts`)

## 설정 파일

`app/nrm-brand.config.json` — `displayName`은 제품명 유지. 커스텀 빌드 스크립트가 appName으로 덮지 않음.

```bash
cd app && npm run sync:brand
```

상세: `docs/APP-BRAND.md`
